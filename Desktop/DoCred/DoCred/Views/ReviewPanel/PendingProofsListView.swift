import SwiftUI

struct PendingProofsListView: View {
    var body: some View {
        VStack {
            Text("Pending Proofs")
                .font(.title2)
                .padding()
            Spacer()
        }
        .navigationTitle("Review Panel")
    }
} 