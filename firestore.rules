rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporarily allow anyone to read/write for 30 days:
    match /{document=**} {
      allow read, write: if true;
    }
  }
}