specVersion: "v1"
serviceId: "vibeloop"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vibeloop-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
