specVersion: "v1"
serviceId: "vibe-loop"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vibe-loop-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
