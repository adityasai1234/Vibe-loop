import { LucideUser } from "lucide-react"

/**
 * Lightweight wrapper so that other components can import:
 *   import { User } from "@/components/ui/user"
 */
export const User = LucideUser
export default LucideUser
